# HTTP server

Is a one file simple HTTP server built using the RUNE programming language.
To show its potential.

This is Rune release 1.4.

Build with make. Serve by $ ./serve. Get the files on a browser or with curl
going to localhost:8080/index.html
